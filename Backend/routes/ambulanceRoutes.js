// import express from "express";
// import {
//   updateLocation,
//   getNearbySignals,
//   createAmbulance,
//   getAmbulance
// } from "../controllers/ambulanceController.js";
// import { protect } from "../middlewares/authMiddleware.js";

// const router = express.Router();

// router.post("/", protect, createAmbulance); // create ambulance record
// router.put("/location", protect, updateLocation); // body: { vehicleId, lat, lng }
// router.get("/:vehicleId", protect, getAmbulance);
// router.get("/near-signals/:vehicleId", protect, getNearbySignals);

// export default router;
